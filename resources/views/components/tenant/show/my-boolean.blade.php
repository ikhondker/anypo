<div class="row mb-3">
	<div class="col-sm-3 text-end">
		<span class="h6 text-secondary">{{ $label }} X:</span>
	</div>
	<div class="col-sm-9">
	   <span class="badge {{ ($value ? 'bg-primary-light' : 'bg-primary-light') }}">{{ ($value ? 'Yes' : 'No') }}</span>
	</div>
 </div>

 
